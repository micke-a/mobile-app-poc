Using the .spec/constitution.md file setup up this basic project into the suitable gradle build used by best practices projects to create a multi platform mobile application using Flutter aiming iOS and Android.

# Step 1: setup the basic project
Basic project structure and setup with the correct gradle configuration to build a flutter application.

Ensure the project builds correctly before step is considered complete.

# Step 2: setup basic screens.
Look at the constitution.md file and create the data model and screens required to display the content.

# Step 3: ensure the project builds correctly.
Ensure the project builds correctly before step is considered complete.